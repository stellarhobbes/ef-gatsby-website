import * as React from "react";
import NavbarHeader from "../components/navbar-header";

const IndexPage = () => {
  return (
    <main class>
      <NavbarHeader />
    </main>
  );
};

export default IndexPage;
